"""
Feature engineering module (owned by Feature engineering owner).
Adds derived features, optionally drops/selects columns.
Exposes: add_features(df) -> DataFrame with engineered features
"""
# TODO: Integrate teammate's features.add_features() when merged
# Placeholder: pass-through until real implementation is available

import pandas as pd


def add_features(df: pd.DataFrame) -> pd.DataFrame:
    """
    Add engineered features to cleaned DataFrame.
    Drops or selects features if needed.
    """
    # Placeholder: return as-is until feature engineering owner merges
    return df.copy()
